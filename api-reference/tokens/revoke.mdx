---
sidebarTitle: Revoke
openapi: "DELETE /v1/auth/api-tokens/{tokenName}"
---

<RequestExample>

```bash cURL
curl -L -X POST 'https://api.turso.tech/v1/v1/auth/api-tokens/{tokenName}' \
-H 'Authorization: Bearer TOKEN'
```

```ts Node.js
import { createClient } from "@turso/api";

const turso = createClient({
  organization: "...",
  token: "",
});

const response = await turso.apiTokens.revoke("my-token");
```

</RequestExample>

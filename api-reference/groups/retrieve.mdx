---
sidebarTitle: Retrieve
openapi: "GET /v1/organizations/{organizationName}/groups/{groupName}"
---

<RequestExample>

```bash cURL
curl -L 'https://api.turso.tech/v1/organization/{organizationName}/groups/{groupName}' \
-H 'Authorization: Bearer TOKEN'
```

```ts Node.js
import { createClient } from "@turso/api";

const turso = createClient({
  organization: "...",
  token: "",
});

const group = await turso.groups.retrieve("default");
```

</RequestExample>

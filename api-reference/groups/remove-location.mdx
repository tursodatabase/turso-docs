---
sidebarTitle: Remove Location
openapi: "DELETE /v1/organizations/{organizationName}/groups/{groupName}/locations/{location}"
---

<RequestExample>

```bash cURL
curl -L -X DELETE 'https://api.turso.tech/v1/organizations/{organizationName}/groups/{groupName}/locations/{location}' \
-H 'Authorization: Bearer TOKEN'
```

```ts Node.js
import { createClient } from "@tursodatabase/api";

const turso = createClient({
  org: "...",
  token: "",
});

const group = await turso.groups.removeLocation("default", "ams");
```

</RequestExample>

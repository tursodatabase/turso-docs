---
sidebarTitle: List
openapi: "GET /v1/organizations/{organizationName}/groups"
---

<RequestExample>

```bash cURL
curl -L https://api.turso.tech/v1/organizations/{organizationName}/groups \
  -H 'Authorization: Bearer TOKEN'
```

```ts Node.js
import { createClient } from "@turso/api";

const turso = createClient({
  organization: "...",
  token: "",
});

const groups = await turso.groups.list();
```

</RequestExample>

---
sidebarTitle: Update Configuration
openapi: "PATCH /v1/organizations/{organizationSlug}/groups/{groupName}/configuration"
---

<RequestExample>

```bash cURL
curl -L -X PATCH 'https://api.turso.tech/v1/organizations/{organizationSlug}/groups/{groupName}/configuration' \
  -H 'Authorization: Bearer TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{
      "delete_protection": true
  }'
```

</RequestExample>

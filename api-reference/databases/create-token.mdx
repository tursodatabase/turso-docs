---
sidebarTitle: Create Token
openapi: "POST /v1/organizations/{organizationName}/databases/{databaseName}/auth/tokens"
---

<RequestExample>

```bash cURL
curl -L -X POST 'https://api.turso.tech/v1/organization/{organizationName}/databases/{databaseName}/auth/tokens?expiration=2w&authorization=full-access' \
-H 'Authorization: Bearer TOKEN'
```

```ts Node.js
import { createClient } from "@turso/api";

const turso = createClient({
  organization: "...",
  token: "",
});

const token = await turso.databases.createToken("my-db", {
  expiration: "2w",
  authorization: "full-access",
});
```

</RequestExample>

<ResponseExample>

```json
{
  "jwt": "TOKEN"
}
```

</ResponseExample>

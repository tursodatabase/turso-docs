---
sidebarTitle: Delete
openapi: "DELETE /v1/organizations/{organizationName}/databases/{databaseName}"
---

<RequestExample>

```bash cURL
curl -L -X DELETE 'https://api.turso.tech/v1/organization/{organizationName}/databases/{databaseName}' \
-H 'Authorization: Bearer TOKEN'
```

```ts Node.js
import { createClient } from "@turso/api";

const turso = createClient({
  organization: "...",
  token: "",
});

const database = await turso.databases.delete("my-db");
```

</RequestExample>

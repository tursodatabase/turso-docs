---
sidebarTitle: Update Configuration
openapi: "PATCH /v1/organizations/{organizationSlug}/databases/{databaseName}/configuration"
---

<RequestExample>

```bash cURL
curl -L -X PATCH 'https://api.turso.tech/v1/organizations/{organizationSlug}/databases/{databaseName}/configuration' \
  -H 'Authorization: Bearer TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{
      "size_limit": "500mb"
  }'
```

</RequestExample>

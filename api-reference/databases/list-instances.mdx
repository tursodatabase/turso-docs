---
sidebarTitle: List Instances
openapi: "GET /v1/organizations/{organizationName}/databases/{databaseName}/instances"
---

<RequestExample>

```bash cURL
curl -L https://api.turso.tech/v1/organizations/{organizationName}/databases/{databaseName}/instances \
  -H 'Authorization: Bearer TOKEN'
```

```ts Node.js
import { createClient } from "@turso/api";

const turso = createClient({
  organization: "...",
  token: "",
});

const instances = await turso.databases.listInstances("my-db");
```

</RequestExample>

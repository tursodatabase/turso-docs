---
sidebarTitle: Retrieve
openapi: "GET /v1/organizations/{organizationName}/databases/{databaseName}"
---

<RequestExample>

```bash cURL
curl -L 'https://api.turso.tech/v1/organization/{organizationName}/databases/{databaseName}' \
-H 'Authorization: Bearer TOKEN'
```

```ts Node.js
import { createClient } from "@turso/api";

const turso = createClient({
  organization: "...",
  token: "",
});

const database = await turso.databases.retrieve("my-db");
```

</RequestExample>
